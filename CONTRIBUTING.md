# Contribuindo

Pull requests são bem-vindos. Para mudanças grandes, abra uma issue antes para
discutir o que você pretende alterar.

Antes de abrir o PR:

```bash
npm install
npm test
```

Os testes cobrem a API (integração), agendamento, upload e validação.
