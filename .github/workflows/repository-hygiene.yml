name: Repository hygiene

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: repository-hygiene-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    name: Pinned Actions baseline
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Verify pinned Actions
        shell: bash
        run: |
          set -euo pipefail

          unpinned="$(git grep -n -E '^[[:space:]]*uses:[[:space:]]*[^[:space:]#]+@' -- .github/workflows \
            | grep -vE '@[0-9a-fA-F]{40}([[:space:]]|#|$)' || true)"
          if [[ -n "$unpinned" ]]; then
            echo 'Found an unpinned GitHub Action:'
            printf '%s\n' "$unpinned"
            exit 1
          fi
